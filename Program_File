import string
import os

def clear_console():
    #allows me to clear the console
    os.system('cls' if os.name == 'nt' else 'clear')

fine_registry = {
    #dictionary which stores last names (as a outer key), first name(str) 
    #fine (int), max speed limit (int) and Wanted (Boolean)

    "Doe":{"first_name": "John","fine":30, "speed":58 , "max speed limit": 50, "wanted (True or False)": "False", "license plate": "HQG739910"},
    "Smith":{"first_name": "Jane", "fine":80, "speed":67, "max speed limit": 50, "wanted (True or False)": "True", "license plate": "AT428722"},
    "Anderson":{"first_name": "Michael","fine":30, "speed":65 , "max speed limit": 60, "wanted (True or False)": "False", "license plate": "VHT823010"},
    "Wong":{"first_name": "Emily","fine":30, "speed":44, "max speed limit": 40, "wanted (True or False)": "False", "license plate": "LKP102958"}
}

def home_page ():

    clear_console()
    valid_menu_input = ("1", "2", "3", "4", "5")
    print("What would you like to do? (use num for input)")
    menu = input ("1.Record Offender, 2.View All Offenders, 3.Search Specific Offender, 4.Display Patrol Summary, 5.Exit Program:\n")

    #checks if 
    if menu not in valid_menu_input:

        return home_page()


    elif menu == "1" :

        clear_console()
        return new_fine_last_name()


    elif menu == "2" :

        clear_console()
        return view_offenders()


    elif menu == "3" :

        return new_student_name()


    elif menu == "4" :

        return patrol_summary()


    elif menu == "5" :

        return exit_program()
       
def new_fine_last_name():

    #creates a new student profile that will be stored on students_scores (includes name only)
    global fine_last_name
    global fine_first_name

    fine_last_name = (input("Enter a last name (or type 'leave' to exit): ").title())

    if fine_last_name == "Leave":

        home_page()

    elif fine_last_name.strip(string.whitespace) or string.punctuation:
        
        new_fine_last_name()

    else:
        new_fine_first_name()

def new_fine_first_name():

    fine_first_name = (input("Enter a first name (or type 'leave' to exit): ").title())

    if fine_first_name == "Leave":
           
        home_page()

    elif fine_first_name.strip(string.whitespace) or string.punctuation:
        
        new_fine_first_name()


    else:
        #moves onto function which does all the calculations for issuing fines
        new_speed_limit ()
   

def new_speed_limit ():

    #this function calculates the amount that a offender
    #has done over the speed limit and issues the correct amount of fine for said offender
    speed_limit = input("Please enter road speed limit (km/h):")
    
    if speed_limit.isalpha():

        new_speed_limit()
        clear_console()

    else:
        new_driver_speed()
    
    
def new_driver_speed():
    speed_driver = int(input("Please enter drivers speed (km/h):"))


    over_speed = int (speed_driver - speed_limit)

    if over_speed.isalpha ():

        new_driver_speed()


    elif 1 <= over_speed <= 10:

        fine_amount = 30
       

    elif 11 <= over_speed <= 20:

        fine_amount = 80
       

    elif 21 <= over_speed <= 30:

        fine_amount = 170
       

    elif 31 <= over_speed <= 40:

        fine_amount = 400
       
   
    elif over_speed > 40:

        fine_amount = 630
       
       
    fine_registry[fine_last_name] = {
       
        "first_name": fine_first_name,
        "fine": fine_amount,
        "speed": over_speed,
        "max speed limit": speed_limit
        #"license plate": fine_license
       
    }
   
    home_page()



def view_offenders():

   
    for key, value in fine_registry.items():
        print(f"Last Name: {key} , First name: {value['first_name']} , Fine amount: ${value['fine']} ,Speed (km/h): {value['speed']} , Road Limit (km/h): {value['max speed limit']} , License: {value['license plate']}" )
   


def patrol_summary():
    print ("works")


#catalist for the rest of the code

home_page()
